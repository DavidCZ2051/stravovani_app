// packages
import 'package:flutter/material.dart';

class AllergensScreen extends StatelessWidget {
  const AllergensScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/seznam_alergenů.jpg",
    );
  }
}
